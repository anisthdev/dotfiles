# Launching
bind = SUPER, space, exec, rofi -show drun

# Aesthetics
bind = SUPER SHIFT, SPACE, exec, pkill -SIGUSR1 waybar
bind = SUPER CTRL, SPACE, exec, ~/.local/bin/rofi-wallpaper-picker

# Notifications
bind = SUPER, comma, exec, makoctl dismiss
bind = SUPER SHIFT, comma, exec, makoctl dismiss --all
bind = SUPER CTRL, comma, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"

# Power menu controls lock, suspend, relaunch, restart, shutdown
bind = SUPER, ESCAPE, exec, ~/.local/bin/rofi-power-menu-2

# Toggle idling
bind = SUPER CTRL, I, exec, ~/.local/bin/toggle-idle

# Screenshots
bind = , PRINT, exec, ~/.local/bin/rofi-screenshot-2

# Screenrecords
bind = ALT, PRINT, exec, ~/.local/bin/rofi-screenrecord

# Color picker
bind = SUPER, PRINT, exec, hyprpicker -a

# Clipboard Managerfont=SF Mono:size=15
bind = SUPER, V, exec, foot -o 'font=SF Mono:size=12' --app-id=clipse -e clipse

# Nerd fonts selector
bind = SUPER CTRL, N, exec, rofi -modi nerdy -show nerdy -theme $HOME/.config/rofi/nerdfont-selector.rasi

# Network launcher
bind = SUPER, N, exec, ~/.local/bin/rofi-wifi-only
bind = SUPER SHIFT, N, exec, ~/.local/bin/rofi-network-controls

# Scratchpads
bind = SUPER, grave, exec, ~/.local/bin/scratchpad term scratchpad-term "uwsm app -- foot --title=scratchpad-term"
bind = SUPER, apostrophe, exec, ~/.local/bin/scratchpad notes scratchpad-notes "uwsm app -- foot --title=scratchpad-notes -e nvim -u $HOME/.config/nvim/lean.lua $HOME/notes/notes.md"
bind = SUPER, A, exec, ~/.local/bin/scratchpad cody scratchpad-cody "uwsm app -- foot --title=scratchpad-cody -o 'font=SF Mono:size=12' -e /home/asif/Projects/lumen/cody/.venv/bin/python -m cody"
bind = SUPER, C, exec, ~/.local/bin/scratchpad opencode scratchpad-opencode "uwsm app -- foot --title=scratchpad-opencode -o 'font=SF Mono:size=12' -e /home/asif/.opencode/bin/opencode"
