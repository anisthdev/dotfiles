#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
THEME="$SCRIPT_DIR/styles/play-video.rasi"

url="$(rofi -dmenu -p "󰌷 " -mesg "    Play" -theme "$THEME")"
url="$(printf '%s' "$url" | xargs)"

[[ -n "$url" ]] || exit 0

exec mpv -- "$url"
