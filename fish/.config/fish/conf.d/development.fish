set ANDROID_SDK_HOME $HOME/.sdks/android
set ANDROID_AVD_HOME $ANDROID_SDK_HOME/avd
set PATH $PATH:$ANDROID_SDK_HOME/cmdline-tools/latest/bin
set PATH $PATH:$ANDROID_SDK_HOME/platform-tools
set PATH $PATH:$ANDROID_SDK_HOME/emulator
